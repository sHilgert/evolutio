<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-lg-12">
    <h2><i class="fa fa-search"></i> Busca</h2>
  </div>
</div>

<div class="wrapper wrapper-content animated fadeInRight">
  <div class = "row">
    <div class = "col-lg-8 col-lg-offset-2 col-xs-12">
      <div class = "ibox float-e-margins">
        <div class = "ibox-content">
          <div class="search">
            <table class="search__table">
              <thead>
                <tr>
                  <th>Nome</th>
                  <th>Cargo</th>
                  <th>Líder</th>
                </tr>
              </thead>
              <tbody>
                <% for user in @users %>
                  <tr>
                    <td><a href="/profile/<%= user.try(:id) %>"><%= user.try(:name) %></a></td>
                    <td><%= user.try(:job).try(:name) %></td>
                    <td><a href="/profile/<%= user.try(:leader).try(:id) %>"><%= user.try(:leader).try(:name) %></a></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
